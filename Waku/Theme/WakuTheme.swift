import SwiftUI

enum WakuTheme {
    static let accent = Color.accentColor
    static let destructive = Color(red: 0.76, green: 0.38, blue: 0.38)
    static let calmAnimation = Animation.smooth(duration: 0.5)
}
